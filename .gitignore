# De plaat `nu/` wordt elke 60 seconden opnieuw gebouwd uit lokale state. Hij
# hoort niet in deze repo: dit is een publieke repo en een pagina die per
# minuut verandert zou hier per minuut een commit opleveren. De bouwer staat
# er wél in (bin/), de uitvoer niet.
nu/
